#include "include/auth.h"
#include <stdio.h>
int register_user(const char* email, const char* password) {
    printf("[STUB] register_user called.\n");
    return 0;
}
int login_user(const char* email, const char* password) {
    printf("[STUB] login_user called.\n");
    return 0;
}
