#ifndef UTILS_H
   #define UTILS_H

   // Helper function to safely copy strings
   void safe_strncpy(char *dest, const char *src, size_t dest_size);

   // Other utility functions can be added here (e.g., logging, error handling wrappers)

   #endif // UTILS_H
